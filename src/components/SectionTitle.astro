---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mt-6 text-3xl font-bold text-center">{title}</div>
<div class="text-2xl font-normal text-gray-500 text-center">{subtitle}</div>
