---
interface SectionTitleType {
  title: string;
  subtitle?: string;
}
const { title } = Astro.props;
const { subtitle } = Astro.props;
---

<div class="mt-6 text-3xl font-bold">{title}</div>
<div class="text-2xl font-normal text-gray-500">{subtitle}</div>
